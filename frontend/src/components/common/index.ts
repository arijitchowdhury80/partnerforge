/**
 * Common Components Index
 *
 * Export all common/shared components for easy importing.
 */

export { SourceBadge, MultipleSources, calculateFreshness } from './SourceBadge';
export { StatusBadge, StatusDot, getStatusFromScore } from './StatusBadge';
export { ScoreGauge, ScoreBar, ScoreChip } from './ScoreGauge';
export {
  LoadingSpinner,
  LoadingOverlay,
  Skeleton,
  CardSkeleton,
  TableRowSkeleton,
} from './LoadingSpinner';
