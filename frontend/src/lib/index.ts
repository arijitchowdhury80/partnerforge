/**
 * Shared Library Exports
 *
 * Import from '@/lib' instead of individual files.
 *
 * Example:
 *   import { COLORS, STATUSES, getStatusFromScore } from '@/lib';
 */

export * from './constants';
