# Arian Frontend Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env files with real values to git!

# =============================================================================
# SUPABASE (Required)
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (public - safe for frontend)
VITE_SUPABASE_KEY=your-anon-key-here

# =============================================================================
# BACKEND API (Optional)
# =============================================================================
# For Vercel deployment: Use /api (Vercel proxies to backend)
# For local development: Use http://localhost:8000
VITE_API_URL=/api

# =============================================================================
# SECURITY WARNINGS
# =============================================================================
#
# DO NOT add these variables to frontend .env files:
#
#   VITE_SUPABASE_SERVICE_KEY - Bypasses RLS! Backend only!
#   VITE_SIMILARWEB_API_KEY   - Paid API, use backend proxy
#   VITE_BUILTWITH_API_KEY    - Paid API, use backend proxy
#   VITE_JSEARCH_API_KEY      - Paid API, use backend proxy
#
# The VITE_ prefix exposes variables to the browser bundle.
# Anyone can view them in DevTools or by inspecting the JS bundle.
#
# Only VITE_SUPABASE_URL and VITE_SUPABASE_KEY should be in frontend.
# =============================================================================
